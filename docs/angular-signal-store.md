## rxMethod

rxMethod turns this `load` right into the function, into the method that can take a value every time this method is called. And what it does internally, it creates the almost observable pipeline and pushes values into that pipeline.
